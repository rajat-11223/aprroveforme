<%= content_for :main do %>

  <h4>Waiting on Response</h4>
  <table class="dashboard-table">
    <thead>
      <tr>
        <th>
          Approver Name
        </th>
        <th>
          Document Name
        </th>
        <th>
          Deadline
        </th>
        <th>
          Action
        </th>
      </tr>
    </thead>
    <tbody></tbody>
    <% @open_responses.each do |approver| %>
      <tr>
        <td class="bold tw-max-w-xs"><%= approver.approval.user.name %></td>
        <td class="tw-max-w-xs"><%= approver.approval.title %></td>
        <td><%= approver.approval.deadline_in_words %></td>

        <td class="listing-inquiry-status">

          <div class="flex-container align-top">
            <div class="flex-child-grow">
              <h4 class="dashboard-table-text"><%= link_to 'Respond', response_path(approver, code: approver.code), class: 'button small round tw-text-black hover:tw-text-black', target: "_blank" %></h4>
            </div>
          </div>

        </td>
      </tr>
    <% end %>
  </tbody>
  </table>

  <% if @open_responses.empty? %>
  <div class="text-center">
    <h3>No Records</h3>
  </div>
  <% end %>

<% end %>
