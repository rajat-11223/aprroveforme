<!--- START NAVIGATION -->
  <div class="top">
    <div class="row">
      <div class="large-6 small-12 columns">
        <p class="logo"><%= link_to (image_tag "logo.png"), root_url %></a></p>
      </div>
      <div class="large-6 small-12 columns">
        <ul class="inline-list right">

               <% if user_signed_in? %>
               <li><%= link_to 'Logout', signout_path, :class=>"login" %></li>
                 <% if current_user.has_role? :admin %>
                  <li><%= link_to 'Admin', users_path %></li>
                 <% end %>
               <% else %>
                  <li><%= link_to 'Login or Signup', signin_path, :class=>"login" %></li>
               <% end %>

               <li><a href="">Support</a></li>
        </ul>
      </div>
    </div>
  </div>
  <!--- END NAVIGATION -->
